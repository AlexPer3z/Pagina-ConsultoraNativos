DirectoryIndex index.php

RewriteEngine On

# Si el archivo o carpeta existe, no redirige
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Redirige todo a index.php
RewriteRule ^(.*)$ index.php [L]